[project]
name = "voicemix-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "jinja2",
    "httpx",
]

[dependency-groups]
dev = ["pytest", "httpx2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
