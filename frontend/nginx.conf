# Serve the built SPA. try_files falls back to index.html so client-side
# routes (deep links) don't 404.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
